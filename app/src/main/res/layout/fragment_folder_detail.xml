<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_app">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <!-- Toolbar floats inside the screen: 20dp top gap (= mini-FAB radius),
             16dp side margins — mirrors the ShortcutEditor's padding="20dp" style -->
        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/topToolbar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp"
            android:layout_marginStart="16dp"
            android:layout_marginEnd="16dp"
            android:backgroundTint="@color/bg_panel"
            android:titleTextColor="@color/text_primary" />

        <TextView
            android:id="@+id/folderHeaderText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingStart="20dp"
            android:paddingTop="14dp"
            android:paddingEnd="20dp"
            android:paddingBottom="6dp"
            android:textColor="@color/text_secondary"
            android:textSize="14sp" />

        <TextView
            android:id="@+id/emptyStateText"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:gravity="center"
            android:padding="24dp"
            android:text="@string/empty_shortcut_entries"
            android:textColor="@color/text_secondary"
            android:visibility="gone" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/shortcutRecyclerView"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:clipToPadding="false"
            android:nestedScrollingEnabled="true"
            android:padding="16dp"
            android:scrollbars="vertical"
            android:scrollbarStyle="insideInset"
            android:fadeScrollbars="true" />
    </LinearLayout>

    <!-- CSV export button — sits at top-right, vertically centred with the toolbar.
         marginTop = toolbar.marginTop(20) + (toolbar.height≈56 - fab.size≈40)/2 ≈ 28dp
         marginEnd matches toolbar's marginEnd so edges align. -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/exportCsvFab"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|end"
        android:layout_marginTop="28dp"
        android:layout_marginEnd="16dp"
        android:contentDescription="@string/action_export_csv"
        android:src="@android:drawable/ic_menu_save"
        app:fabSize="mini" />

    <!-- Add new shortcut button — bottom-right -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/addShortcutFab"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_marginEnd="20dp"
        android:layout_marginBottom="76dp"
        android:contentDescription="@string/action_add_shortcut_entry"
        android:src="@android:drawable/ic_input_add" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
